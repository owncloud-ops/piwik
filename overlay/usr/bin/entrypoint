#!/usr/bin/env sh

set -eo pipefail

/usr/local/bin/gomplate -V -o /etc/php7/php.ini -f /etc/templates/php.ini.tmpl

printf "\nStarting Matomo ...\n"

supercronic -split-logs /etc/crontabs/nginx 1>/dev/null &
php-fpm7 -F &
exec nginx -g "daemon off;"
